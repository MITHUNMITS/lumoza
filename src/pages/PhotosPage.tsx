export function PhotosPage() {
  return (
    <div className="rounded-[24px] border border-white/8 bg-card/70 p-6 text-sm text-muted">
      The dedicated Photos page will be expanded once the scan pipeline and virtualized data source are wired.
    </div>
  );
}
